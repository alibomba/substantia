type UserSettings = {
    avatar: string | null,
    banner: string | null,
    hasChannel: boolean,
    username: string,
    slug: string,
    facebook: string | null,
    instagram: string | null,
    twitter: string | null,
    description: string | null,
    email: string
}